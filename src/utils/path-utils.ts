export const arraysEqual = <A extends any[], B extends A>(a: A, b: B) => {
  if (a.length !== b.length) return false;
  for (let i = 0; i < a.length; i++) {
    if (a[i] !== b[i]) return false;
  }
  return true;
};
